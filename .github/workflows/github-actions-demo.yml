
name: actions-test

on: [push]
jobs: 
  check-bats-version: # 잡의 이름
    runs-on: ubuntu-latest # VM 이름
    steps: # 순서
      - uses: actions/checkout@v3 
      - uses: actions/setup-node@v3
      with:
        node-version: '14'
      - run: npm install -g bats
      - run: bats -v