

name: Vue Build
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
      - name: Checkout source code.
        uses: actions/checkout@master
      - name: install npm
        run: npm install
      - name: build
        run: npm run build
